@model IEnumerable<WebGridUsandoAspNet.Models.Categoria>


@{
    ViewBag.Title = "ListarCategoria";

    var grid = new WebGrid(source: Model, canPage: true, rowsPerPage: 10);
    grid.Pager(WebGridPagerModes.All);

}

<h2>ListarCategoria</h2>

<style type="text/css">
  
        .mainpanel {
            margin-left: 230px;
            min-height: 636px;
            padding: 20px;
            padding-top: 20px;
            padding-right: 20px;
            padding-bottom: 20px;
            padding-left: 20px;
        }

        .info th {
            background: #E0E0E0 !important;
        }

        .info th a {
            color: #000000;
            font-weight: normal;
        }

</style>


<div class="table-responsive">

    @grid.GetHtml(
    tableStyle: "table table-bordered",
    headerStyle: "info",
    footerStyle: "webgrid-footer",
    alternatingRowStyle: "table table-hover",
    selectedRowStyle: "webgrid-selected-row",
    rowStyle: "gridrow",

     columns: grid.Columns(
             grid.Column(columnName: "Id", header: "Controle"),
             grid.Column(columnName: "Nome", header: "Nome Categoria"),
             grid.Column(columnName: "Status", header: "Status")
             //grid.Column(columnName: "", header: "Edição", format: (item) =>
             //  {
             //      var link = Html.ActionLink("Edit", "Edit", new { id = item.Id });
             //      return link;
             //  }, style: "btn btn-default"),
             //grid.Column(columnName: "", header: "Deletar", format: (item) =>
             //  {
             //      var link = Html.ActionLink("Edit", "Edit", new { id = item.Id });
             //      return link;
             //  }, style: "btn btn-default")


          ));

</div>